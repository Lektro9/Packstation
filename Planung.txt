1. Pakete kommen in den Größen: XS, S, M , L, XL, XXL

2. Entsprechende Paketfächer sind einzurichten

3. Paketstation können zwischen 9 und 100 Fächer besitzen

4. Paketstationen können bzgl. ihrer Anzahl der Paketfächer erweitert werden.

5. Es gibt zwischen 1 und 100 Paketstationen im Gesamtsystem!

zu 1.

x - Enum benutzen für die Größen (Werte werden zugeordnet) in der Paketklasse
x - Konstruktoren anpassen (sollen nun den neuen Wert vergeben)

zu 2. 

x - siehe Oben, auch hier Enums benutzen um zu vergleichen (Eigenschaft von Paketfach)
x - nur Fächer mit entsprechender Größe oder größer sollen genutzt werden können (Testmethode erstellt)

zu 3.

x - neue Eigenschaft in der Paketstation Klasse (zb int "AnzahlFaecher")
x - neue Eigenschaft in Konstruktoren einbinden
x - neue "Prüfmethode" in Paketstation erstellen um die Anzahl zwischen 9 und 100 zu gewährleisten, NACHDEM ein Fach hinzugefügt wurde

zu 4.

x - Liste der Faecher in Paketstation.cs sollte erweiterbar sein (Aggregatsfunktionen erstellen) 
x - Variable "AnzahlFaecher" muss aktualisiert und geprüft werden
x - neuen Aggregatsfunktionen erstellen (zb "FachHinzufuegen()" und "FachEntfernen()")

zu 5.

x - neue Eigenschaft für Controller (Liste von Paketstationen) 
x - Paketstation.cs braucht eine neue Eigenschaft um identifiziert zu werden (zb "int ID") x
x - Eigenschaft in Konstruktoren einbauen
x - neuen Aggregatsfunktionen erstellen (zb "StationHinzufuegen()" und "StationEntfernen()")
(- alternativ: einen neuen Controller der auf einzelne Controller (mit Paketstationen) verweist)
x - Variable "AnzahlStationen" muss aktualisiert und geprüft werden
x - Userinterface muss erweitert werden um eine Paketstation auszuwählen 
(TODO: - Userinterface muss erweitert werden um Station zu wechseln?)


//TODO after
x - Fehler abfangen wenn alle Paketfächer belegt sind
x - prüfe Konstruktoren und entferne unnötige
x - Standart Fächer mit unterschiedlichen Größen
x - Test schreiben für mehrere Pakete mit gleichen Größen, die Fächer mit unterschiedlichen Größen nutzen (zb 4 S Pakete -> 3 S Fächer und 1 M Fach)




